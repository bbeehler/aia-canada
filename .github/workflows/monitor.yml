name: Daily Media Monitoring Sync

on:
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch: 
    inputs:
      timeframe:
        description: 'Timeframe for search results'
        required: false
        default: 'qdr:w' # Default to past week
        type: string

jobs:
  run-monitor:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository files
        uses: actions/checkout@v6

      - name: Set up Python environment
        uses: actions/setup-python@v6
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          # Or explicitly force it if your requirements file isn't synced:
          # pip install supabase google-genai requests dateparser

      - name: Execute Monitor and Push to Supabase
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          SERPER_API_KEY: ${{ secrets.SERPER_API_KEY }}
          TIMEFRAME_INPUT: ${{ github.event.inputs.timeframe }}
        run: python fetch_mentions.py
